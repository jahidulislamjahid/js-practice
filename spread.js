// getting just the elements from an array 
//  ... pulls out just the elements from an array.
const numbers = [12, 50, 80, 56, 23, 45, 59, 78, 20];
console.log(...numbers);
// getting the max number from the array
console.log(Math.max(...numbers));